# For the full list of possible targets,
# see the Makefile, particularly the definition of .PHONY.
targets = [ "show_config",
            "show_params",
            "tests",
            "reports",
           ]
